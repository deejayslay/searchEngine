

i) libraries used: pickle, json, nltk, WordNetLemmatizer, wordnet, re, math, defaultdict from collections, BeautifulSoup


ii) "index_constructor.py": creates an inverted index for individual words in the corpus, and also creates a bi-inverted index for bi-grams (2 words)

"basic_query.py": for a given query, finds top 20 results from the corpus and displays  the number of documents that contain that query as well as the urls of those top 20 results

"gui.py": a graphical user interface that takes a given query from a search bar, and displays the results from basic_query.py with a short description for each search result


iii. N/A

